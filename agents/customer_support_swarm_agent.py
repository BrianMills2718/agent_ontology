#!/usr/bin/env python3
"""
Customer Support Swarm — Generated by OpenClaw Instantiation Engine
Spec: Multi-agent customer support with peer-to-peer handoffs
"""

import json
import os
import sys
import time
from datetime import datetime

# ═══════════════════════════════════════════════════════════
# Trace Log
# ═══════════════════════════════════════════════════════════

TRACE = []

def trace_call(agent_label, model, system_prompt, user_message, response, duration_ms):
    entry = {
        "timestamp": datetime.now().isoformat(),
        "agent": agent_label,
        "model": model,
        "system_prompt": system_prompt,
        "user_message": user_message,
        "response": response,
        "duration_ms": duration_ms,
    }
    TRACE.append(entry)
    print(f"    [{agent_label}] ({model}, {duration_ms}ms)")
    print(f"      IN:  {user_message[:300]}")
    print(f"      OUT: {response[:300]}")


def dump_trace(path="trace.json", iterations=0, clean_exit=True):
    # Compute metrics
    total_ms = sum(e["duration_ms"] for e in TRACE)
    agents_used = list(set(e["agent"] for e in TRACE))
    schema_ok = 0
    for e in TRACE:
        try:
            r = e["response"].strip()
            if r.startswith("```"): r = "\n".join(r.split("\n")[1:-1])
            json.loads(r if r.startswith("{") else r[r.find("{"):r.rfind("}")+1])
            schema_ok += 1
        except (json.JSONDecodeError, ValueError):
            pass
    est_input_tokens = sum(len(e.get("user_message",""))//4 for e in TRACE)
    est_output_tokens = sum(len(e.get("response",""))//4 for e in TRACE)
    metrics = {
        "total_llm_calls": len(TRACE),
        "total_duration_ms": total_ms,
        "avg_call_ms": total_ms // max(len(TRACE), 1),
        "iterations": iterations,
        "clean_exit": clean_exit,
        "agents_used": agents_used,
        "schema_compliance": f"{schema_ok}/{len(TRACE)}",
        "est_input_tokens": est_input_tokens,
        "est_output_tokens": est_output_tokens,
    }
    output = {"metrics": metrics, "trace": TRACE}
    with open(path, "w") as f:
        json.dump(output, f, indent=2)
    print(f"\nTrace written to {path} ({len(TRACE)} calls, {total_ms}ms total)")
    print(f"  Schema compliance: {schema_ok}/{len(TRACE)}, est tokens: ~{est_input_tokens}in/~{est_output_tokens}out")
    return metrics

# ═══════════════════════════════════════════════════════════
# LLM Call Infrastructure
# ═══════════════════════════════════════════════════════════

# Model override: set OPENCLAW_MODEL env var to override all agent models at runtime
_MODEL_OVERRIDE = os.environ.get("OPENCLAW_MODEL", "")
_OPENROUTER_API_KEY = os.environ.get("OPENROUTER_API_KEY", "")


def _openrouter_model_id(model):
    """Map spec model names to OpenRouter model IDs (provider/model format)."""
    if "/" in model:
        return model  # already in provider/model format
    if model.startswith("gemini"):
        return f"google/{model}"
    if model.startswith("claude") or model.startswith("anthropic"):
        return f"anthropic/{model}"
    if model.startswith("gpt") or model.startswith("o1") or model.startswith("o3"):
        return f"openai/{model}"
    return model  # pass through as-is


def _call_openrouter(model, system_prompt, user_message, temperature, max_tokens):
    from openai import OpenAI
    client = OpenAI(
        base_url="https://openrouter.ai/api/v1",
        api_key=_OPENROUTER_API_KEY,
    )
    response = client.chat.completions.create(
        model=_openrouter_model_id(model),
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_message},
        ],
        temperature=temperature,
        max_tokens=max_tokens,
    )
    return response.choices[0].message.content


def call_llm(model, system_prompt, user_message, temperature=0.7, max_tokens=4096, retries=3):
    if _MODEL_OVERRIDE:
        model = _MODEL_OVERRIDE
    for attempt in range(retries):
        try:
            if _OPENROUTER_API_KEY:
                return _call_openrouter(model, system_prompt, user_message, temperature, max_tokens)
            elif model.startswith("claude") or model.startswith("anthropic"):
                return _call_anthropic(model, system_prompt, user_message, temperature, max_tokens)
            elif model.startswith("gemini"):
                return _call_gemini(model, system_prompt, user_message, temperature, max_tokens)
            else:
                return _call_openai(model, system_prompt, user_message, temperature, max_tokens)
        except Exception as e:
            if attempt < retries - 1:
                wait = 2 ** attempt
                print(f"    [RETRY] {type(e).__name__}: {e} — retrying in {wait}s (attempt {attempt+1}/{retries})")
                import time as _time; _time.sleep(wait)
            else:
                print(f"    [FAIL] {type(e).__name__}: {e} after {retries} attempts")
                return json.dumps({"stub": True, "model": model, "error": str(e)})


def _call_openai(model, system_prompt, user_message, temperature, max_tokens):
    from openai import OpenAI
    client = OpenAI()
    response = client.chat.completions.create(
        model=model,
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_message},
        ],
        temperature=temperature,
        max_tokens=max_tokens,
    )
    return response.choices[0].message.content


def _call_anthropic(model, system_prompt, user_message, temperature, max_tokens):
    import anthropic
    client = anthropic.Anthropic()
    response = client.messages.create(
        model=model,
        max_tokens=max_tokens,
        system=system_prompt,
        messages=[{"role": "user", "content": user_message}],
    )
    return response.content[0].text


def _call_gemini(model, system_prompt, user_message, temperature, max_tokens):
    from google import genai
    client = genai.Client(api_key=os.environ.get("GEMINI_API_KEY", ""))
    response = client.models.generate_content(
        model=model,
        contents=f"{system_prompt}\n\n{user_message}",
        config={"temperature": temperature, "max_output_tokens": max_tokens},
    )
    return response.text

# ═══════════════════════════════════════════════════════════
# Schema Registry
# ═══════════════════════════════════════════════════════════

SCHEMAS = {
    "TriageResult": {
        "description": """""",
        "fields": [{"name": "intent", "type": "string"}, {"name": "confidence", "type": "float"}, {"name": "summary", "type": "string"}],
    },
    "SupportResponse": {
        "description": """""",
        "fields": [{"name": "answer", "type": "string"}, {"name": "resolved", "type": "boolean"}, {"name": "handoff_needed", "type": "boolean"}, {"name": "handoff_reason", "type": "string"}],
    },
    "Ticket": {
        "description": """""",
        "fields": [{"name": "id", "type": "string"}, {"name": "request", "type": "string"}, {"name": "intent", "type": "string"}, {"name": "response", "type": "string"}, {"name": "resolved", "type": "boolean"}, {"name": "agent_path", "type": "list<string>"}],
    },
    "ConversationTurn": {
        "description": """""",
        "fields": [{"name": "speaker", "type": "string"}, {"name": "role", "type": "string"}, {"name": "content", "type": "string"}, {"name": "turn_number", "type": "integer"}],
    },
}


def validate_output(data, schema_name):
    """Validate parsed LLM output against schema. Returns list of issues."""
    schema = SCHEMAS.get(schema_name)
    if not schema or "raw" in data:
        return []
    issues = []
    for field in schema["fields"]:
        fname = field["name"]
        ftype = field["type"]
        if fname not in data:
            issues.append(f"Missing field '{fname}' ({ftype})")
            continue
        val = data[fname]
        if ftype == "string" and not isinstance(val, str):
            issues.append(f"Field '{fname}' expected string, got {type(val).__name__}")
        elif ftype == "integer" and not isinstance(val, (int, float)):
            issues.append(f"Field '{fname}' expected integer, got {type(val).__name__}")
        elif ftype.startswith("list") and not isinstance(val, list):
            issues.append(f"Field '{fname}' expected list, got {type(val).__name__}")
        elif ftype.startswith("enum["):
            allowed = [v.strip() for v in ftype[5:-1].split(",")]
            if val not in allowed:
                issues.append(f"Field '{fname}' value '{val}' not in {allowed}")
    if issues:
        print(f"    [SCHEMA] {schema_name}: {len(issues)} issue(s): {issues[0]}")
    return issues


def build_input(state, schema_name):
    """Build an input dict for an agent call using schema field names.
    Pulls matching keys from state.data, checking both flat keys and
    values nested inside dict entries (e.g. state.data["xxx_input"]["field"])."""
    schema = SCHEMAS.get(schema_name)
    if not schema:
        return state.data
    result = {}
    for field in schema["fields"]:
        fname = field["name"]
        if fname in state.data:
            result[fname] = state.data[fname]
        else:
            # Search nested dicts in state.data for the field
            for _k, _v in state.data.items():
                if isinstance(_v, dict) and fname in _v:
                    result[fname] = _v[fname]
                    break
    return result


def output_instruction(schema_name):
    """Generate a JSON output instruction string for the LLM."""
    schema = SCHEMAS.get(schema_name)
    if not schema:
        return ""
    fields = ", ".join(f'"{f["name"]}": <{f["type"]}>' for f in schema["fields"])
    return f"\n\nRespond with ONLY valid JSON matching this schema: {{{fields}}}"


def parse_response(response, schema_name):
    """Parse an LLM response according to an output schema.
    Returns a dict of field values, or {"raw": response} if parsing fails."""
    import re as _re
    text = response.strip()
    # Handle markdown code blocks
    if text.startswith("```"):
        lines = text.split("\n")
        lines = lines[1:]  # skip ```json
        if lines and lines[-1].strip() == "```":
            lines = lines[:-1]
        text = "\n".join(lines)
    # Attempt 1: direct parse
    try:
        parsed = json.loads(text)
        if isinstance(parsed, dict):
            return parsed
        return {"value": parsed}
    except json.JSONDecodeError:
        pass
    # Attempt 2: extract JSON object from prose
    start = text.find("{")
    end = text.rfind("}") + 1
    if start >= 0 and end > start:
        candidate = text[start:end]
        try:
            return json.loads(candidate)
        except json.JSONDecodeError:
            pass
        # Attempt 3: fix trailing commas
        fixed = _re.sub(r",\s*}", "}", candidate)
        fixed = _re.sub(r",\s*]", "]", fixed)
        try:
            return json.loads(fixed)
        except json.JSONDecodeError:
            pass
        # Attempt 4: find matching braces (handle nested)
        depth = 0
        for i, ch in enumerate(text[start:], start):
            if ch == "{": depth += 1
            elif ch == "}": depth -= 1
            if depth == 0:
                try:
                    return json.loads(text[start:i+1])
                except json.JSONDecodeError:
                    break
    return {"raw": response}


# ═══════════════════════════════════════════════════════════
# Stores
# ═══════════════════════════════════════════════════════════

class Store_ticket_store:
    """Ticket Store (kv)"""
    def __init__(self):
        self.data = {}

    def read(self, key=None):
        return self.data if key is None else self.data.get(key)

    def write(self, value, key=None):
        if key is not None:
            self.data[key] = value
        else:
            self.data = value



# ═══════════════════════════════════════════════════════════
# Agent Wrappers
# ═══════════════════════════════════════════════════════════

def invoke_triage_agent(user_message, output_schema=None):
    """Triage Agent"""
    system = """You are a customer support triage agent. Analyze the customer's request and determine which specialist to route to. Classify the intent as: billing, technical, account, or general.
"""
    if output_schema:
        system += output_instruction(output_schema)
    t0 = time.time()
    result = call_llm(
        model="gemini-3-flash-preview",
        system_prompt=system,
        user_message=user_message,
        temperature=0.7,
        max_tokens=4096,
    )
    trace_call("Triage Agent", "gemini-3-flash-preview", system, user_message, result, int((time.time()-t0)*1000))
    return result


def invoke_billing_agent(user_message, output_schema=None):
    """Billing Agent"""
    system = """You are a billing specialist. Help customers with invoices, payments, refunds, and subscription changes. If the request is outside your scope, indicate a handoff back to triage.
"""
    if output_schema:
        system += output_instruction(output_schema)
    t0 = time.time()
    result = call_llm(
        model="gemini-3-flash-preview",
        system_prompt=system,
        user_message=user_message,
        temperature=0.7,
        max_tokens=4096,
    )
    trace_call("Billing Agent", "gemini-3-flash-preview", system, user_message, result, int((time.time()-t0)*1000))
    return result


def invoke_tech_support_agent(user_message, output_schema=None):
    """Tech Support Agent"""
    system = """You are a technical support specialist. Help customers with product issues, bugs, setup, and troubleshooting. If the request is outside your scope, indicate a handoff back to triage.
"""
    if output_schema:
        system += output_instruction(output_schema)
    t0 = time.time()
    result = call_llm(
        model="gemini-3-flash-preview",
        system_prompt=system,
        user_message=user_message,
        temperature=0.7,
        max_tokens=4096,
    )
    trace_call("Tech Support Agent", "gemini-3-flash-preview", system, user_message, result, int((time.time()-t0)*1000))
    return result


def invoke_account_agent(user_message, output_schema=None):
    """Account Agent"""
    system = """You are an account management specialist. Help customers with profile updates, password resets, and account settings. If the request is outside your scope, indicate a handoff back to triage.
"""
    if output_schema:
        system += output_instruction(output_schema)
    t0 = time.time()
    result = call_llm(
        model="gemini-3-flash-preview",
        system_prompt=system,
        user_message=user_message,
        temperature=0.7,
        max_tokens=4096,
    )
    trace_call("Account Agent", "gemini-3-flash-preview", system, user_message, result, int((time.time()-t0)*1000))
    return result



# ═══════════════════════════════════════════════════════════
# Agent State
# ═══════════════════════════════════════════════════════════

class AgentState:
    """Runtime state for Customer Support Swarm"""
    def __init__(self):
        self.ticket_store = Store_ticket_store()
        self.data = {}  # current data flowing through the pipeline
        self.iteration = 0
        self.schema_violations = 0


# ═══════════════════════════════════════════════════════════
# Process Functions
# ═══════════════════════════════════════════════════════════

def process_intake(state):
    """
    Receive Request
    """
    print(f"  → Receive Request")

    # Logic from spec
    state.data["request"] = state.data.get("query", "")
    state.data["conversation_history"] = []
    if state.data.get("_done"):
        return state

    return state


def process_triage(state):
    """
    Triage Request
    """
    print(f"  → Triage Request")

    # Logic from spec
    pass
    if state.data.get("_done"):
        return state

    # Invoke: triage_agent
    triage_agent_input = state.data.copy()
    triage_agent_msg = json.dumps(triage_agent_input, default=str)
    triage_agent_raw = invoke_triage_agent(triage_agent_msg, output_schema="TriageResult")
    triage_agent_result = parse_response(triage_agent_raw, "TriageResult")
    # Validate and merge output fields into state.data
    state.schema_violations += len(validate_output(triage_agent_result, "TriageResult"))
    state.data.update(triage_agent_result)
    state.data["triage_result"] = triage_agent_result
    state.data["triage_result"] = triage_agent_result
    print(f"    ← Triage Agent: {triage_agent_result}")

    return state


def process_route(state):
    """
    Route to Specialist
    """
    print(f"  → Route to Specialist")

    # Gate: intent classification
    # Branch: intent == billing → handle_billing
    # Branch: intent == technical → handle_tech
    # Branch: intent == account → handle_account

    if state.data.get("intent") == "billing":
        return "handle_billing"
    elif state.data.get("intent") == "technical":
        return "handle_tech"
    elif state.data.get("intent") == "account":
        return "handle_account"
    else:
        return "respond_general"


def process_handle_billing(state):
    """
    Handle Billing
    """
    print(f"  → Handle Billing")

    # Logic from spec
    pass
    if state.data.get("_done"):
        return state

    # Invoke: billing_agent
    billing_agent_input = state.data.copy()
    billing_agent_msg = json.dumps(billing_agent_input, default=str)
    billing_agent_raw = invoke_billing_agent(billing_agent_msg, output_schema="SupportResponse")
    billing_agent_result = parse_response(billing_agent_raw, "SupportResponse")
    # Validate and merge output fields into state.data
    state.schema_violations += len(validate_output(billing_agent_result, "SupportResponse"))
    state.data.update(billing_agent_result)
    state.data["support_response"] = billing_agent_result
    state.data["handle_billing_result"] = billing_agent_result
    print(f"    ← Billing Agent: {billing_agent_result}")

    return state


def process_handle_tech(state):
    """
    Handle Tech Support
    """
    print(f"  → Handle Tech Support")

    # Logic from spec
    pass
    if state.data.get("_done"):
        return state

    # Invoke: tech_support_agent
    tech_support_agent_input = state.data.copy()
    tech_support_agent_msg = json.dumps(tech_support_agent_input, default=str)
    tech_support_agent_raw = invoke_tech_support_agent(tech_support_agent_msg, output_schema="SupportResponse")
    tech_support_agent_result = parse_response(tech_support_agent_raw, "SupportResponse")
    # Validate and merge output fields into state.data
    state.schema_violations += len(validate_output(tech_support_agent_result, "SupportResponse"))
    state.data.update(tech_support_agent_result)
    state.data["support_response"] = tech_support_agent_result
    state.data["handle_tech_result"] = tech_support_agent_result
    print(f"    ← Tech Support Agent: {tech_support_agent_result}")

    return state


def process_handle_account(state):
    """
    Handle Account
    """
    print(f"  → Handle Account")

    # Logic from spec
    pass
    if state.data.get("_done"):
        return state

    # Invoke: account_agent
    account_agent_input = state.data.copy()
    account_agent_msg = json.dumps(account_agent_input, default=str)
    account_agent_raw = invoke_account_agent(account_agent_msg, output_schema="SupportResponse")
    account_agent_result = parse_response(account_agent_raw, "SupportResponse")
    # Validate and merge output fields into state.data
    state.schema_violations += len(validate_output(account_agent_result, "SupportResponse"))
    state.data.update(account_agent_result)
    state.data["support_response"] = account_agent_result
    state.data["handle_account_result"] = account_agent_result
    print(f"    ← Account Agent: {account_agent_result}")

    return state


def process_respond_general(state):
    """
    General Response
    """
    print(f"  → General Response")

    # Logic from spec
    state.data["response"] = state.data.get("answer", "I'll help you with that.")
    if state.data.get("_done"):
        return state

    return state


def process_finalize(state):
    """
    Save & Respond
    """
    print(f"  → Save & Respond")

    # Logic from spec
    state.data["_done"] = True
    if state.data.get("_done"):
        return state

    # Write: Save ticket
    state.ticket_store.write(state.data.copy())

    return state


# ═══════════════════════════════════════════════════════════
# State Machine Executor
# ═══════════════════════════════════════════════════════════

PROCESSES = {
    "intake": process_intake,
    "triage": process_triage,
    "route": process_route,
    "handle_billing": process_handle_billing,
    "handle_tech": process_handle_tech,
    "handle_account": process_handle_account,
    "respond_general": process_respond_general,
    "finalize": process_finalize,
}

TRANSITIONS = {
    "intake": "triage",
    "triage": "route",
    # "route": determined by gate logic
    "handle_billing": "finalize",
    "handle_tech": "finalize",
    "handle_account": "finalize",
    "respond_general": "finalize",
    "finalize": None,  # terminal
}


MAX_ITERATIONS = int(os.environ.get("OPENCLAW_MAX_ITER", "100"))


def run(initial_data=None):
    """Customer Support Swarm — main execution loop"""
    state = AgentState()
    if initial_data:
        state.data.update(initial_data)

    current = "intake"
    print(f"\n════════════════════════════════════════════════════════════")
    print(f"  Customer Support Swarm")
    print(f"  Multi-agent customer support with peer-to-peer handoffs")
    print(f"════════════════════════════════════════════════════════════\n")

    while current and state.iteration < MAX_ITERATIONS:
        state.iteration += 1
        print(f"\n[Iteration {state.iteration}] State: {current}")

        process_fn = PROCESSES.get(current)
        if not process_fn:
            print(f"  Unknown process: {current}")
            break

        result = process_fn(state)

        if current in ['route']:
            current = result
        else:
            current = TRANSITIONS.get(current)

        # Fan-out: if transition is a list, run all branches sequentially
        while isinstance(current, list):
            _targets = current
            for _ft in _targets:
                state.iteration += 1
                print(f"\n[Iteration {state.iteration}] State: {_ft} (fan-out)")
                _fn = PROCESSES.get(_ft)
                if _fn:
                    _fn(state)
            # Collect unique next-hops from all fan-out targets
            _next_set = []
            for _ft in _targets:
                _nt = TRANSITIONS.get(_ft)
                if _nt is not None and _nt not in _next_set:
                    _next_set.append(_nt)
            current = _next_set[0] if len(_next_set) == 1 else (_next_set if _next_set else None)

        if current is None or state.data.get("_done"):
            print("\n  [DONE] Reached terminal state.")
            break

    _clean_exit = state.iteration < MAX_ITERATIONS
    if state.iteration >= MAX_ITERATIONS:
        print(f"\n  [STOPPED] Max iterations ({MAX_ITERATIONS}) reached.")
        _clean_exit = False

    if state.schema_violations > 0:
        print(f"\n  [SCHEMA] {state.schema_violations} total schema violation(s) during execution")
    dump_trace(iterations=state.iteration, clean_exit=_clean_exit)
    print(f"\nFinal state.data keys: {list(state.data.keys())}")
    return state


if __name__ == "__main__":
    run()