# Core (required for validation, analysis, and spec tools)
pyyaml>=6.0

# LLM providers (required for running generated agents)
google-genai>=1.0.0
openai>=1.0.0
anthropic>=0.25.0

# Agent runtime
chromadb>=1.0.0

# LangGraph backend (optional — only needed for --backend langgraph)
langgraph>=0.1.0
langchain-core>=0.1.0
langchain-google-genai>=1.0.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
